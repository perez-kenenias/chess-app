# Backend de ajedrez — dependencias Python
#
# fastapi       → Framework web moderno y rápido (reemplaza Flask en nuevos proyectos)
# uvicorn       → Servidor ASGI que corre FastAPI (equivalente a Tomcat para Java)
# python-chess  → Maneja tablero, reglas, validación FEN y comunicación UCI con Stockfish
# pydantic      → Validación de datos (ya viene con FastAPI, se lista para claridad)
# anthropic     → SDK de Claude para generar comentarios de GM en lenguaje natural
# python-dotenv → Carga las claves de API desde el archivo .env

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-chess>=1.999
pydantic>=2.0.0
anthropic>=0.96.0
python-dotenv>=1.0.0
